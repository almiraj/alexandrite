export class TimeRow {
  begin:String
  end:String
  interval:String
  constructor(begin:String, end:String, interval:String) {
    this.begin = begin;
    this.end = end;
    this.interval = interval;
  }
}
